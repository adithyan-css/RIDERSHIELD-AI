name: ridershield
description: RiderShield AI — Smart Rider Safety App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  web_socket_channel: ^3.0.1
  provider: ^6.1.2
  http: ^1.2.1
  flutter_blue_plus: ^1.32.12
  flutter_tts: ^4.0.2
  geolocator: ^12.0.0
  shared_preferences: ^2.2.3
  fl_chart: ^0.68.0
  permission_handler: ^11.3.1
  intl: ^0.19.0
  uuid: ^4.4.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
